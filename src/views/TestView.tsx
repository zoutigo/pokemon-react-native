import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const TestView = () => {
  return (
    <View>
      <Text>here is test area</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default TestView;
